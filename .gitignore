node_modules
dist
dist-ssr
*.tsbuildinfo
*.local
# secrets — never commit API credentials
.env
.DS_Store
.vite
coverage
# generated PWA artifacts
dev-dist
# data pipeline fetch cache (the generated dataset itself IS committed)
scripts/data/.cache
# Claude Code personal settings (local guardrails, not shared)
.claude/settings.local.json
